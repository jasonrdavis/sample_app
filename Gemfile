source 'http://rubygems.org'
gem 'rails', '3.0.3'

gem 'sqlite3-ruby', :require => 'sqlite3'

# author line instead is
# gem 'sqlite3-ruby', '1.2.5', :require => 'sqlite3'
# I am keeping the original line instead to see what happens.

group :development do
  gem 'rspec-rails', '2.1.0'
end

group :test do
  gem 'rspec', '2.1.0'
  gem 'webrat', '0.7.1'
end


